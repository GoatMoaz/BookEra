<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="description" content="">
    <meta name="author" content="">

    <link rel="stylesheet" href="/stylesheets/log.css" type="text/css">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <title>Login Page</title>
</head>

<style>
    body, html {
        margin: 0;
        overflow: hidden;
    }

    img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        position: fixed;
        top: 0;
        left: 0;
        z-index: -1;
    }

</style>
<body>

<%- include('partials/navbar') %>

<% if (success.length > 0) { %>
    <div class="alert alert-success">
        <%= success %>
    </div>
<% } %>
<% if (error.length > 0) { %>
    <div class="alert alert-danger">
        <%= error %>
    </div>
<% } %>
<div class="header animate__animated animate__fadeInDown">
    <div style="font-weight: bold;position: relative;bottom: 60px">BOOK<span>ERA</span></div>
</div>
<img src="/images/b2.png" alt="">
<div class="login animate__animated animate__fadeInDown">
    <% if (user) { %>
        <h1 class="text-white">WELCOME BACK <%= user.username %></h1>
        <a class="btn btn-primary" href="/users/logout">LOG OUT</a>
    <% } else { %>
        <form action="/users/login" method="POST" style="position:relative;bottom: 60px">
            <input name="username" placeholder="Username" type="text" />
            <input name="password" placeholder="Password" type="password" />
            <button class="button-64" role="button"><span class="text">Log In</span></button>
        </form>
            <p style="color: white;position:relative;bottom: 25px">Don't have an account? <a href="/users/signup" class="login_link">Signup</a> </p>
    <% } %>
</div>
</body>
