<!DOCTYPE html>
<html>
<head>
    <title>Change Password</title>
    <link rel="stylesheet" href="/stylesheets/style.css" type="text/css" />
    <link rel="stylesheet" href="/stylesheets/log.css" type="text/css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>

</head>
<body>
    <% if (success.length > 0) { %>
        <div class="alert alert-success">
            <%= success %>
        </div>
    <% } %>
    <% if (error.length > 0) { %>
        <div class="alert alert-danger" style="position: absolute; z-index: 2;top: 0;width: 100%;font-weight: bold;">
            <%= error %>
        </div>
    <% } %>
    <div class="banner">
        <div class="wrapper">
            <form action="/users/change-password" method="post">
                <h1>Change Password</h1>
                <div class="input-box">
                    <input type="password" id="old_password" name="old_password"placeholder="Old Password" required>
                    <i class='bx bxs-lock-alt'></i>
                </div>
                <div class="input-box">
                    <input type="password" id="new_password" name="new_password" placeholder="New Password"required>
                    <i class='bx bxs-lock-alt' ></i>
                </div>
                <button type="submit" class="btn" role="button">Change Password</button>
            </form>
        </div>
    </div>
</body>
</html>
