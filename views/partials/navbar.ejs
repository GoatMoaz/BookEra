<!DOCTYPE html>

<html lang="en">

<head>
    <title></title>
    <meta charset="utf-8">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Saira:wght@500;600;700&display=swap"
          rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/icons.css" type="text/css">
    <link href="/stylesheets/boot.css" rel="stylesheet" type="text/css">
    <link href="/stylesheets/nav.css" rel="stylesheet" type="text/css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<div class="container-fluid " style="background-color: black">
    <div class="container">
        <nav class="navbar navbar-dark navbar-expand-lg py-0">
            <h1 class="text-white fw-bold d-block">BOOK<span style="color: #5379fa">ERA</span></h1>
            <!-- Toggle/collapsible Button -->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon" style="color: #5379fa"></span>
            </button>

            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <div class="navbar-nav ms-auto mx-xl-auto p-0">
                    <!-- Navbar links-->
                    <form action="/books/search" method="GET">
                        <div class="search-box">
                            <button type="submit" class="btn-search"><i class="bi-search nav-item nav-link"></i></button>
                            <input type="text" name="title" class="input-search" placeholder="Search books, authors, publishers and categories...">
                        </div>
                    </form>
                    <a href="/" class="nav-item nav-link">Home</a>
                    <a href="/books" class="nav-item nav-link">Books</a>
                    <a href="/about" class="nav-item nav-link">About Us</a>
                </div>

                <div class="navbar-nav">
                    <% if (!user) { %>
                        <a href="/users/login" class="nav-item nav-link">Login</a>
                        <a href="/users/signup" class="nav-item nav-link">Signup</a>
                    <% } else { %>
                        <!-- Add a link to the user's profile page -->
                        <a href="/users/<%= user._id %>" class="nav-item nav-link"> <%= user.username %> </a>
                        <a href="/users/logout" class="nav-item nav-link">Logout</a>
                        <% if (user.type === 'buyer')  { %>
                            <a href="#" id="cart-icon"><i class="bi bi-cart nav-item nav-link"></i></a>
                        <% } %>
                    <% } %>
                </div>
            </div>
        </nav>
    </div>
</div>
</body>

</html>